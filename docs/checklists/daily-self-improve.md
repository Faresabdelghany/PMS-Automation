# Daily Self-Improve Checklist (Cron 6:00 AM)

1. Read:
   - `SOUL.md`, `USER.md`
   - `memory/YYYY-MM-DD.md` (today + yesterday)
   - `MEMORY.md`

2. If today’s daily file is missing, create it with:
   - `# Memory — YYYY-MM-DD`

3. Extract 1 concrete improvement (prompts/workflow) and apply it only if:
   - Low-risk + reversible (docs/checklists/prompt text)
   - Does NOT change auth/routing/runtime/cost

4. Consistency check:
   - Outgoing summaries must follow **Message Prefix Rule**: `Ziko ⚡:`

5. Log actions:
   - Append today’s daily memory entry
   - Push a PMS event via `scripts/push-event.ps1`
