# Memory — 2026-02-28

## Major Session Outcomes

- Enabled and tested OpenAI Codex integration in OpenClaw.
- Ziko model switching tested; PMS/OpenClaw model sync behavior debugged multiple times.
- Added OpenAI-Codex models into PMS `user_models` and fixed agent edit UI to include `openai-codex` provider + model list.
- Fixed PMS build/deploy blockers from TypeScript/schema mismatch:
  - removed invalid `session_key` usage in `lib/actions/agents.ts`
  - patched `command_type` typing mismatch
- Confirmed deployment and validated agent activity panel fix in browser with test account.

## Agent Architecture Decision (Final)

- Fares finalized lean architecture with **6 agents**:
  - Ziko
  - Product Analyst
  - Marketing Agent (on-call)
  - Designer (on-call)
  - Dev
  - Code Reviewer
- Workflow is strict and autonomous, no approvals:
  - `Fares -> Ziko -> Product Analyst -> Dev -> Code Reviewer -> Ziko -> Fares`
- Hard rule reinforced: **Ziko must not dispatch Dev directly before Product Analyst handoff**.

## PMS / Task Orchestration Work

- Ran Product Analyst -> Dev -> Reviewer workflow to build task orchestration v1.
- Migrations produced and later applied (via Dev with Supabase MCP):
  - `20260227000001_task_orchestration_v1.sql`
  - `20260227000002_agent_events_task_create_types.sql`
- Reviewer approved orchestration v1 behavior.
- Follow-up bug fix cycle done for agent detail panel activities (data source/wiring issues).
- Task visibility bug for agent-event-created tasks diagnosed and fixed in API path.

## Realtime Activity Feed / Mirroring

- Identified that activity feed gaps were due to missing guaranteed infra-level mirroring.
- Added activity mirror daemon in workspace scripts.
- Fixed startup script failures and relaunched daemon.
- Patched daemon so unmapped/unknown events are not dropped (fallback routing).
- Policy updated: all meaningful agent actions should mirror to PMS events.

## Skills / Marketplace

- Skills marketplace sync issue investigated and fixed pipeline initiated (hardcoded 8 -> dynamic/fallback catalog behavior).
- Installed requested skills in PMS:
  - gog
  - self-improving-agent
  - find-skills
  - github
  - humanizer
- Assigned skills first by role, then per Fares request assigned **all available skills to all 6 agents**.

## Automation / Cron

- Added daily self-improve cron at **6:00 AM Cairo** (`self-improve-6am`).
- Daily standup delivery failures surfaced; manual follow-up messages sent.

## Communication/Platform Notes

- Telegram does not support OpenClaw slash command UX (`/clear`, etc.) in the same way as CLI/web surfaces.
- Fares requested context reset but first to save everything to memory.

## Persistent Preference/Behavior Updates

- Keep replies concise and direct.
- Enforce strict orchestration chain with Product Analyst gate before Dev.
- Ensure PMS is source of truth for activities/tasks and mirror updates automatically.
