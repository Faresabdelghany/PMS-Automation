# 2026-03-04

## Gmail API (gogcli) setup
- Installed gogcli to `C:\Tools\gog\gog.exe` and added to user PATH (new shells).
- Enabled Gmail API in Google Cloud project `gen-lang-client-0535159229`.
- Created OAuth Desktop client: "OpenClaw Gmail (gogcli)".
- OAuth token must be created per account via:
  - `& "C:\Tools\gog\gog.exe" auth add fares4389@gmail.com --services gmail`
- Draft creation verified working via API:
  - `gog gmail drafts create ...`
  - Test draft created to self:
    - subject: "OpenClaw Draft Test"
    - draftId: `r7120767434840780160`

## Notes / guidance for Abdallah (Job Search workflow)
- Stop relying on Gmail UI autosave; create drafts via Gmail API using `gog gmail drafts create`.
- Add acceptance check: draft exists (draftId returned) before reporting success.
- Never use/paste Gmail password; use OAuth only.

## Job search preferences (Fares)
- Mode: FULL-AUTO approved; cap 10 applications/day.
- Target titles: Senior Project Manager, Project Manager, Lead PM, Engineering PM, Delivery Manager.
- Locations:
  - Egypt: remote/hybrid/onsite OK
  - KSA/UAE: remote only
  - Worldwide: remote only
- Industries to avoid: Construction, Civil, Pharma, Education, Telecom Infrastructure.
- Salary (EXACT values from Fares):
  - Current salary: 60,000 EGP/month
  - Target salary: 70,000 EGP/month
  - (Remote outside Egypt target previously stated: $2,000/month)
- Notice period: 1 week.
- Resume handling: do NOT upload/attach local CV during applications; use platform-saved resume. If resume upload is mandatory and no saved resume is available, skip and log.
- If salary not disclosed: apply only if role looks clearly senior/credible; otherwise skip and log "salary not disclosed".
- Application routing rule (EXACT from Fares):
  - If LinkedIn **Easy Apply** exists → apply via LinkedIn.
  - If application is via **company website/ATS** → apply there and complete **all steps**.
  - Do **not** skip just because it redirects off LinkedIn.
  - CV file to upload when required: `C:\Users\Fares\Documents\Abdelaziz resume.pdf`
- Screening answers rule: use CV + conversation context + common sense to answer screening questions. If genuinely unknown with no reasonable default, ask once — never refuse or block.

## Job applications status (live)
- Run snapshot: applied=10, skipped=7 (from applications_log.md + tester summary).
- Applied companies list (first batch): Adree, Waffarha, Advansys, Infoplus Technologies UK Limited, Idexcel, iBrain Technologies, Gravity IT Resources, Troy Consulting LLC, Actionstep, ALTEN Delivery Centre Spain.
- Geo rule update after those: Egypt + Remote MENA only; US excluded.
- Latest instruction: continue with another 10 applications under the new geo rules.

## Clarification added (2026-03-04)
- Apply **from LinkedIn** only, using LinkedIn’s 2 options:
  - **Easy Apply** OR **Website** (redirect to ATS).
- **Do not skip any**; only skip if a hard blocker prevents submission (e.g., captcha loop, mandatory resume upload with no saved resume available, login wall). Always log the exact reason + link, then replace with another job to still hit the daily cap.
