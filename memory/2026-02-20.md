# 2026-02-20 — First Session

## What happened
- Bootstrapped identity: I'm **Ziko**, human is **Abdelaziz** (goes by **Fares**)
- Set up via Telegram (`@Fares_Agents_bot`)
- Migrated auth from Anthropic API key → **Claude Max OAuth** (setup-token flow)
  - Nuked old auth (auth-profiles.json, .env, openclaw.json auth section)
  - Added new `anthropic:manual` token profile
  - Cleaned out stale `anthropic:clwd` profile from agent-level auth store
- Changed default model from `claude-sonnet-4-6` → **`claude-opus-4-6`**
  - Added full `models.providers.anthropic` config with baseUrl (no /v1), cost, context window, etc.
- Gateway runs on `localhost:18789`, no service manager — runs as a bare node process
- Telegram channel: polling mode, works fine
- PowerShell execution policy blocks `.ps1` scripts — use `openclaw.cmd` or `cmd /c` as workaround

## Mission Control (Next.js dashboard)
- Path: `C:\Users\Fares\Projects\mission-control`
- Next.js 15 (downgraded from 16 due to Turbopack Windows bug with PostCSS)
- Pages: Dashboard, Agents, Tasks, Notes, Goals, Tools, PMS, Settings
- PMS bridge: connected to Supabase, pulls live project/task/client data
- API routes: `/api/pms`, `/api/feed`, `/api/data`
- Dev server on port 3000

## PMS Project
- Path: `C:\Users\Fares\Downloads\PMS`
- Full Project Management SaaS (Next.js 16 + Supabase)
- Live at: https://pms-dashboard-tau.vercel.app
- Supabase: lazhmdyajdqbnxxwyxun.supabase.co
- 21 projects, 241 tasks, 10 clients, 12 members
- Test creds: e2e-test@example.com (not storing password)

## Notes
- Fares is hands-on, gives detailed step-by-step instructions
- OS: Windows 10 (19045), x64
- Gateway has no scheduled task or service — needs manual start or `openclaw.cmd gateway`
- Next.js 16 Turbopack has a Windows bug: tries to read `nul` file path via PostCSS — downgrade to 15 fixes it
