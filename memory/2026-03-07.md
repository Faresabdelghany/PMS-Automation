# 2026-03-07

## Session start (05:28 Cairo)
- Created today's daily memory file.
- Reviewed 2026-03-06 notes: full automation pipeline built (auto-intake, auto-assign, auto-dispatch, 10-min sweep, comment handoffs, activity tracking, Supabase CLI).

## DB Schema Expansion (05:30 Cairo)
- Pushed 3 migrations to Supabase (all via `npx supabase db push`):
  - **Phase 1** (`20260307053000`): `agent_runs` table + `task_events` table + extended `agent_logs` (run_id, event_type, message, details_json, level, error_message, completed_at)
  - **Phase 2** (`20260307053001`): `tool_invocations` table + extended `todos` (current_run_id, last_event_at, failed_at, completed_at, archived_at) + indexes + safer source uniqueness (channel+message_id)
  - **Phase 3** (`20260307053002`): `skill_invocations` table
- All tables have RLS enabled with anon policy, `agent_runs` added to Realtime publication.
- Existing tables (`todos`, `subtasks`, `comments`, `agent_logs`) preserved intact.
- Architecture: agent_runs = execution sessions, agent_logs = log lines inside runs, task_events = user-facing timeline, tool/skill_invocations = observability.

## Script Wiring to New Schema (05:38 Cairo)
- **New scripts created:**
  - `create-agent-run.ps1` — creates agent_runs row, returns run_id
  - `update-agent-run.ps1` — updates run status/output/error/completed_at
  - `emit-task-event.ps1` — inserts task_events row (event_type, actor, summary, metadata)
- **Updated scripts:**
  - `log-agent-task.ps1` — now accepts RunId, EventType, Level, ErrorMessage; sets completed_at
  - `update-todo.ps1` — now accepts CurrentRunId, SetLastEventAt, SetCompletedAt, SetFailedAt, SetArchivedAt switches
  - `intake-request.ps1` — now emits `task_created` event after todo creation
  - `task-watcher.ps1` — now creates `agent_runs` (queued) + links to todo + emits `task_claimed` event on claim
- **Updated DISPATCH-FOOTER.md** — full 9-step execution protocol:
  1. Create agent_run → 2. Emit task_started → 3. Do work → 4. Update todo → 5. Complete run → 6. Emit completion event → 7. Post comment → 8. Log to agent_logs → 9. Reply
- Smoke tested: create-agent-run + update-agent-run both confirmed working.

## Memory Search Fix Attempt (06:30 Cairo)
- QMD indexed and embedded 18 docs locally (hybrid BM25+vector, no external embedding needed)
- Added Gemini API key to gateway.cmd + auth-profiles.json
- Problem: OpenClaw's memory_search tool tries Gemini embeddings even with QMD backend, hits 404 (bug in OpenClaw code path)
- Workaround: Use `memory_get` to read MEMORY.md directly instead of `memory_search` tool
- QMD CLI works fine locally (`qmd search "query"` returns results) — the issue is in OpenClaw's internal wrapper, not QMD itself
