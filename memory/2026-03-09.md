# Memory — 2026-03-09

## 6:00 AM Self-Improve Routine
- Reviewed 2026-03-08 memory (task classification hardening, live Projects integration, build discipline), 2026-03-07 memory (observability schema expansion, DISPATCH-FOOTER 9-step protocol), and MEMORY.md.
- Found version drift: runtime default_model updated to `gpt-5.3-codex` but MEMORY.md, SOUL.md, and dev.md prompt still referenced `gpt-5.2-codex`.
- Applied fix: updated all 3 files (MEMORY.md, SOUL.md, agents/prompts-v2/dev.md) to `gpt-5.3-codex`.
- Safe, reversible change. No functional risk — aligns documentation with actual runtime config.

## 5:07 PM — PA→Dev→Tester→Reviewer Workflow Contract Implemented

### What was built
- **DB migration** `20260309022500_child_task_workflow_contract.sql` pushed: adds `parent_task_id`, `order_index`, `source`, `acceptance_criteria`, `lifecycle_status` to todos + unique constraints enforcing one-active-child-per-parent and idempotent speckit imports.
- **Ingestion script** `ingest-speckit-decomposition.ps1`: single approved path for PA to persist SpecKit tasks into PMS. First task = ready, rest = queued. Idempotent by (parent_task_id, order_index, source=speckit).
- **PA gate** `verify-pa-spec-gate.ps1`: blocks Dev if spec.md/plan.md/tasks.md missing or child queue invalid.
- **Transition enforcer** `transition-child-task.ps1`: role-based transitions, sequential unlock (PA marks done → next task becomes ready). Writes agent_log on every transition.
- **Orchestrator** `run-feature-workflow.ps1`: single entrypoint routing PA→Dev→Tester→Reviewer→Complete.
- **Docs**: `INGESTION-SCHEMA.md`, `REVIEWER-POLICY.md`, `WORKFLOW-PA-DEV-TEST-REVIEW.md`.

### Lifecycle statuses
queued → ready → in_progress → dev_done → in_test → tested_passed → in_review → done (+ changes_requested, failed, cancelled)

### Reviewer policy
- Low-risk: tested_passed → done (fast-track)
- Standard/high-risk: tested_passed → in_review → done

### Known bug
- `emit-task-event.ps1` fails on metadata JSON with special chars (ConvertFrom-Json pipe issue). Task_events from transitions missing; agent_logs work fine. Needs fix.

### E2E test passed
- Real parent + 2 child tasks ran full cycle through Dev→Test→PA→Reviewer with sequential unlock confirmed and 12 agent_log rows written.

### Fares feedback
- Got frustrated with repeated stage-1 reports. Must deliver delta-only going forward — no restating completed work.
