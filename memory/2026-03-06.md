# 2026-03-06

## Self-improve routine (6am)
- Created today's daily memory file.
- Reviewed 2026-03-05 notes + MEMORY.md.
- Finding: This cron job (`self-improve-6am`) still references "log to PMS via push-event" but PMS was dropped entirely on 2026-03-06 per MEMORY.md changes log. Cron description is stale.
- Improvement applied: Added browser automation resilience note to MEMORY.md Key Lessons based on yesterday's LinkedIn timeout failures (0/65 applications submitted due to browser control timeouts).

## Supabase Logging STOP-GATE Enforcement (06:09 Cairo)
- Fares flagged that agents complete tasks without logging to Supabase despite the rule existing in prompts.
- Root cause: logging section was buried at the bottom of each prompt, easy for sub-agents to skip.
- Fix applied to all 5 agent prompts (dev, product-analyst, testing-agent, code-reviewer, job-search):
  - Renamed section to `⛔ Supabase Agent Logging — HARD GATE (STOP-GATE)`
  - Added explicit 4-step sequence: run → verify output → retry on failure → ONLY THEN reply
  - Added consequence: "If you skip this step, the entire task is considered NOT DONE"
  - Language escalated from "non-negotiable" to "HIGHEST-PRIORITY RULE. IT OVERRIDES EVERYTHING ELSE."
- Also updated SOUL.md and AGENTS.md with matching STOP-GATE language + Ziko enforcement duty.
- Verified with live test: Ziko log row confirmed in Supabase (id: 7b8ae5ba, 04:09:36 UTC).

## PMS-Automation Tasks Page Integration (07:30 Cairo)
- Wired existing Tasks page in `C:\Users\Fares\PMS-Automation` to real agent system.
- DB migration: added `todo_id` FK on `agent_logs` → `todos` for linking agent activity to tasks.
- Updated assignee options to real 8 agents (was placeholder names).
- Wired Activities tab in TaskDetailsPanel to fetch real agent_logs from Supabase.
- Updated comment @mentions to real agents + Fares.
- All existing UI views (List, Board, Kanban) preserved untouched.

## Hybrid Task Pickup System (19:24 Cairo)
- Built dual-mode task pickup: Supabase Realtime + heartbeat polling.
- DB migration: added `claimed_by` + `claimed_at` columns on `todos` for de-duplication.
- DB migration: enabled Supabase Realtime on `todos` table.
- `scripts/task-realtime.mjs` — Node.js long-running Realtime subscriber, wakes Ziko via webhook on new dispatchable tasks.
- `scripts/task-watcher.ps1` — PowerShell poll script, finds unclaimed tasks assigned to agents, claims them, returns JSON.
- HEARTBEAT.md updated to run task-watcher on every heartbeat cycle.
- Live test: poll script correctly found and claimed a task assigned to Ziko ("Coordinate system review and sprint planning").

## Auto-intake + workflow enforcement (21:30 Cairo)
- Implemented Option B: added workflow tracking fields to `todos`: source_message_id, source_channel, source_ts, workflow_stage (PA/DEV/TEST/REVIEW/SPECIALIST) + unique index on source_message_id.
- Added `subtasks` table with RLS policies; TaskDetailsPanel now persists subtasks in DB.
- Updated `log-agent-task.ps1` to REQUIRE TodoId; logs fail without linked task.
- Dispatch footer now requires TodoId and mandates updating `todos` status/workflow_stage before logging.
- Created helper scripts: create-todo.ps1, update-todo.ps1, intake-request.ps1, create-subtask.ps1.
- Auto-intake now creates a todo per inbound message (deduped by source_message_id) and sets workflow_stage=PA.
- Verified Tasks page shows new auto-intake tasks and subtask persistence in Task Detail.

## Auto-dispatch + 10-min status sweep (23:00 Cairo)
- Added realtime auto-dispatch: task-realtime.mjs now runs task-watcher.ps1 on dispatchable tasks and wakes Ziko.
- Implemented task-status-sweep.ps1: scans open todos, pulls latest comment/log, outputs progress lines.
- Set OpenClaw cron job `task-status-sweep` to fire every 10 minutes with system event RUN_STATUS_SWEEP.
- Updated DISPATCH-FOOTER.md to require task comment handoffs with @mentions.
- Design doc saved: docs/plans/2026-03-06-auto-dispatch-status-sweep-design.md
- Requirement: no auto-close, use comments as handoff trail; auto-dispatch includes Ziko.

## Auto-assign by intent (22:40 Cairo)
- Created `scripts/auto-assign.ps1` — keyword-based intent classifier that maps task titles to agents.
- Mapping: design→Designer, test→Testing Agent, review→Code Reviewer, marketing→Marketing Agent, job→Job Search Agent, research→Product Analyst, build→Dev, default→Ziko.
- Auto-assign always wins over manual assignment (per Fares's decision).
- Integrated into `intake-request.ps1` (runs on task creation) and `task-realtime.mjs` (runs on INSERT/UPDATE).
- HEARTBEAT.md updated: auto-assign runs before task-watcher on every heartbeat.

## Activity tracking fields + comment handoff format (00:45 Cairo, Mar 7)
- DB migration: added 5 structured fields on `todos`:
  - `created_by_user`, `created_by_agent` (who created the task)
  - `updated_by_user`, `updated_by_agent` (who last updated)
  - `last_update_summary` (what changed)
- Migration ran via Supabase browser SQL editor (future: use `npx supabase db push`).
- Updated `create-todo.ps1` → accepts CreatedByUser + CreatedByAgent params.
- Updated `update-todo.ps1` → accepts UpdatedByUser + UpdatedByAgent + LastUpdateSummary params.
- Updated `intake-request.ps1` → defaults CreatedByUser="Fares", CreatedByAgent="Ziko".
- Created `scripts/add-comment.ps1` — inserts comments into `comments` table with author + html.
- Updated DISPATCH-FOOTER.md with enforced comment format:
  - Summary of work done
  - @NextAgent mention for pipeline handoff
  - Pipeline order: PA → Dev → Testing Agent → Code Reviewer → Ziko
  - Must call `update-todo.ps1` with updated_by fields before logging
- Updated `ProjectTask` type in `lib/data/project-details.ts` with new fields.
- Updated `TodoRow` type and `rowToTask` mapper in `lib/services/tasks.ts`.
- Updated `TaskDetailsPanel.tsx` Activities tab to display:
  - "Created by [user] via [agent]"
  - "Updated by [agent] — [summary]"
  - "Workflow stage [badge]"
- Verified on UI: screenshot shows "Updated by Ziko" + "Workflow stage TEST" + agent logs.

## Supabase CLI (00:55 Cairo, Mar 7)
- `npx supabase` works (v2.77.0); no global install available via winget/scoop.
- Updated TOOLS.md: use `npx supabase db push` for future migrations.

## Key decisions captured today
- Auto-assign always wins over manual assignment.
- Auto-dispatch triggers immediately on auto-assign (including Ziko tasks).
- 10-min cron status sweep (no summaries to user; just check progress internally).
- No auto-close of tasks; agents report in Task Comments with @mentions.
- created_by/updated_by are structured (separate user + agent fields).
- Supabase CLI via `npx supabase` for future DB work.
