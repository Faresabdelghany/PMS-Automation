# 2026-03-08

## Session start (01:13 Cairo)
- Created today's daily memory file.
- Reviewed 2026-03-07 notes: DB schema expansion (agent_runs, task_events, tool/skill_invocations), script wiring, DISPATCH-FOOTER 9-step protocol, memory search workaround.

## 6:00 AM Self-Improve Routine
- Reviewed 2026-03-07 memory, today's memory, and MEMORY.md.
- Found redundancy: "Changes Log" section at bottom of MEMORY.md duplicated content already in structured sections (Automation Pipeline, Observability Schema).
- Applied: removed Changes Log section (~350 tokens saved per context load). Safe, reversible.

## Tasks + Projects hardening milestone (06:30–06:49 Cairo)
- Completed task classification hardening end-to-end:
  - `todos.task_type` hardened with NOT NULL + strict check constraint (`user_task|agent_task|system_task`).
  - Added/used `v_user_tasks` for safe default user-facing task queries.
  - Updated creation paths to pass/validate `TaskType` explicitly in scripts and UI service flows.
  - Reclassified leaked historical `RUN_STATUS_SWEEP` rows from `user_task` to `system_task` and verified zero leak into `v_user_tasks`.
- Completed live Projects integration:
  - Added `projects` table and `todos.project_id` FK.
  - Root `/` and `/projects/:id` now read live Supabase-backed project/task data through service layer.
  - Added internal visibility path for non-user tasks in Agents view.
- Completed live project creation:
  - Wired `ProjectWizard` (quick + guided) to real `createProject()` Supabase insert path.
  - After create: refreshes live projects list and routes to `/projects/:id`.
- Timeline production fix:
  - Replaced fixed demo date marker with real current date marker.
- Build discipline improvement:
  - Fixed `app/projects/[id]/backlog/page.tsx` module error; `npx tsc --noEmit` passes.
- Documentation synced to reality:
  - Updated `CLAUDE.md` architecture notes to reflect Supabase-backed tasks/projects and observability tables.
- Commits:
  - `402437a` — task_type hardening + live Projects read integration.
  - `56686ab` — live ProjectWizard persistence + cleanup/docs/TS fix.
